name: Apply compact toolbar fix

on:
  push:
    branches: [polish/compact-chatgpt-toolbar]
    paths:
      - scripts/apply_compact_toolbar_fix.py
      - .github/workflows/apply-compact-toolbar-fix.yml

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: polish/compact-chatgpt-toolbar
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install template validation dependencies
        run: python -m pip install --disable-pip-version-check pyyaml==6.0.3 jsonschema==4.26.0
      - name: Install JavaScript dependencies
        run: npm ci --no-audit --no-fund
      - name: Apply fix
        run: python scripts/apply_compact_toolbar_fix.py
      - name: Remove one-shot files
        run: rm scripts/apply_compact_toolbar_fix.py .github/workflows/apply-compact-toolbar-fix.yml
      - name: Validate, type-check, lint, and build
        run: npm run check
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run browser integration tests
        run: npm run test:e2e
      - name: Commit verified changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix: keep core actions visible in compact layouts"
          git push origin HEAD:polish/compact-chatgpt-toolbar
